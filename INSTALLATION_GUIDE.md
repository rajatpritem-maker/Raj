# সমিতি ম্যানেজমেন্ট সিস্টেম - ইনস্টলেশন গাইড

## 📋 প্রয়োজনীয় প্রয়োজনীয়তা

- PHP 8.0+ (Laravel 9 এর জন্য)
- MySQL 5.7+
- Composer (Dependency Manager)
- cPanel একটি হোস্টিং অ্যাকাউন্ট (অথবা VPS/ডেডিকেটেড সার্ভার)

## 🚀 ইনস্টলেশন ধাপ

### Step 1: ফাইল আপলোড করুন

1. আপনার cPanel ফাইল ম্যানেজারে প্রবেশ করুন
2. `public_html` ফোল্ডারে প্রবেশ করুন
3. সব প্রজেক্ট ফাইল এখানে আপলোড করুন

### Step 2: ডাটাবেস তৈরি করুন

1. cPanel-এ MySQL ডাটাবেস এ প্রবেশ করুন
2. একটি নতুন ডাটাবেস তৈরি করুন (নাম: `somiti_db` অথবা আপনার পছন্দের নাম)
3. একটি নতুন ব্যবহারকারী তৈরি করুন এবং সব প্রিভিলেজ দিন

### Step 3: Composer Dependencies ইনস্টল করুন

cPanel টার্মিনাল/SSH এ প্রবেশ করুন:

```bash
cd public_html
composer install
```

### Step 4: .env ফাইল সেটআপ করুন

```bash
cp .env.example .env
php artisan key:generate
```

### Step 5: ইনস্টলেশন উইজার্ড চালান

আপনার ব্রাউজারে প্রবেশ করুন:
```
http://yoursite.com/install
```

ইনস্টলেশন উইজার্ড অনুসরণ করুন এবং প্রয়োজনীয় তথ্য প্রদান করুন:
- ডাটাবেস সংযোগ বিবরণ
- অ্যাডমিন অ্যাকাউন্ট তথ্য
- সাইট সেটিংস

### Step 6: ডাটাবেস মাইগ্রেশন (যদি প্রয়োজন হয়)

```bash
php artisan migrate
php artisan db:seed
```

অথবা, SQL ফাইল ম্যানুয়ালি আমদানি করুন:
- cPanel এ phpMyAdmin খুলুন
- ডাটাবেস নির্বাচন করুন
- "Import" এ ক্লিক করুন
- `database/somiti_db.sql` ফাইল নির্বাচন করুন

## 🔐 নিরাপত্তা সেটিংস

### File Permissions সেট করুন

```bash
chmod 755 storage
chmod 755 bootstrap/cache
```

### .htaccess কনফিগার করুন

`public` ফোল্ডারে একটি `.htaccess` ফাইল তৈরি করুন:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Remove index.php from URL
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
```

### PHP.ini সেটিংস

আপনার cPanel এ PHP সংস্করণ নিশ্চিত করুন সঠিক:

```ini
max_upload_size = 10M
max_execution_time = 300
memory_limit = 256M
```

## 📚 ব্যবহার

### প্রথম লগইন

1. আপনার ব্রাউজারে প্রবেশ করুন: `http://yoursite.com`
2. "লগইন" এ ক্লিক করুন
3. ইনস্টলেশনে তৈরি করা অ্যাডমিন ইমেল এবং পাসওয়ার্ড ব্যবহার করুন

### অ্যাডমিন প্যানেল

অ্যাডমিন ড্যাশবোর্ড এ: `http://yoursite.com/admin/dashboard`

### ব্যবহারকারী রেজিস্ট্রেশন

- ব্যবহারকারীরা "নিবন্ধন" এ ক্লিক করে নতুন অ্যাকাউন্ট তৈরি করতে পারেন
- তাদের জাতীয় পরিচয়পত্র বা জন্ম সনদ আপলোড করতে হবে
- অ্যাডমিন অনুমোদনের পরে তারা সম্পূর্ণ অ্যাক্সেস পাবেন

## 🔧 প্রধান বৈশিষ্ট্যসমূহ

### ব্যবহারকারী পক্ষ
- ✅ আত্মপরিচয় যাচাইকরণ (NID/Birth Certificate)
- ✅ ট্র্যাকিং কোড সিস্টেম
- ✅ ডিপিএস সঞ্চয় প্রোগ্রাম
- ✅ ঋণ আবেদন এবং ব্যবস্থাপনা
- ✅ লেনদেন ইতিহাস
- ✅ আইডি কার্ড জেনারেশন (PNG/PDF)
- ✅ চ্যাট সিস্টেম
- ✅ বহু-ভাষা সমর্থন (বাংলা/ইংরেজি)

### অ্যাডমিন পক্ষ
- ✅ ব্যবহারকারী অনুমোদন/প্রত্যাখ্যান
- ✅ খবর, নোটিশ, কার্যক্রম পরিচালনা
- ✅ ডিপিএস স্কিম পরিচালনা
- ✅ ঋণ অনুমোদন
- ✅ লেনদেন রিপোর্ট
- ✅ সাইট সেটিংস

## 📁 ফাইল স্ট্রাকচার

```
project/
├── app/
│   ├── Models/
│   │   ├── User.php
│   │   ├── News.php
│   │   ├── DpsAccount.php
│   │   ├── Loan.php
│   │   └── ...
│   └── Http/
│       └── Controllers/
│           ├── AuthController.php
│           ├── DashboardController.php
│           ├── AdminController.php
│           └── ...
├── config/
│   ├── app.php
│   └── database.php
├── database/
│   └── somiti_db.sql
├── resources/
│   ├── lang/
│   │   ├── bn.php
│   │   └── en.php
│   └── views/
│       ├── auth/
│       ├── user/
│       ├── admin/
│       └── ...
├── routes/
│   └── web.php
└── public/
    ├── .htaccess
    └── index.php
```

## 🔐 নিরাপত্তা বৈশিষ্ট্য

- ✅ CSRF সুরক্ষা
- ✅ SQL ইনজেকশন প্রতিরোধ
- ✅ পাসওয়ার্ড হ্যাশিং
- ✅ ফাইল আপলোড যাচাইকরণ
- ✅ ব্যবহারকারী অনুমোদন মিডলওয়্যার
- ✅ এন্ক্রিপ্ট করা ডাটাবেস সংযোগ

## 🐛 সমস্যা সমাধান

### সাদা পৃষ্ঠা ত্রুটি
1. `bootstrap/cache` পরিষ্কার করুন
2. `php artisan config:cache` চালান

### ডাটাবেস সংযোগ ত্রুটি
1. `.env` ফাইল সঠিকভাবে সেট আছে কিনা যাচাই করুন
2. ডাটাবেস ব্যবহারকারীর অনুমতি যাচাই করুন

### ফাইল আপলোড ত্রুটি
1. `storage` ফোল্ডারের অনুমতি যাচাই করুন (755)
2. PHP `max_upload_size` সেটিং যাচাই করুন

### লগইন সমস্যা
1. অ্যাডমিন অ্যাকাউন্ট সঠিকভাবে তৈরি হয়েছে কিনা যাচাই করুন
2. ডাটাবেস `users` টেবিল সঠিকভাবে তৈরি হয়েছে কিনা যাচাই করুন

## 📞 সহায়তা

যেকোনো সমস্যার জন্য নিম্নলিখিত যাচাই করুন:

1. সমস্ত প্রয়োজনীয় ফাইল আপলোড করা হয়েছে কিনা
2. ডাটাবেস সঠিকভাবে তৈরি এবং কনফিগার করা হয়েছে কিনা
3. PHP সংস্করণ এবং এক্সটেনশন সমর্থিত কিনা
4. সার্ভার লগ চেক করুন অতিরিক্ত তথ্যের জন্য

## 📝 লাইসেন্স

এই প্রজেক্টটি হল একটি কাস্টম বিকাশ। সমস্ত অধিকার সংরক্ষিত।

---

**Version:** 1.0
**Last Updated:** 2024
**Language:** বাংলা/English
